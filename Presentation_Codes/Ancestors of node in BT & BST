int ancestors(struct node * root,int data){
    if(root==NULL) return 0;
    if(root->data==data){
        return 1;
    }
    
    if(ancestors(root->left,data)||ancestors(root->right,data)){
        printf("%d ",root->data);
        return 1;
    }
    
    return 0;
}
